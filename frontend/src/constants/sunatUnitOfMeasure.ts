/**
 * Unidades mínimas SUNAT (cat. 03) usadas en el formulario simplificado.
 * Servicio: ZZ y NIU. Producto (bien): solo NIU.
 */
export const SUNAT_SERVICE_UNITS: { code: string; label: string }[] = [
  { code: 'ZZ', label: 'ZZ — UNIDAD (SERVICIOS)' },
  { code: 'NIU', label: 'NIU — UNIDAD (BIENES)' },
];

export const SUNAT_PRODUCT_UNIT_LABEL = 'NIU — UNIDAD (BIENES)';
